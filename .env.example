# Host of the deployed PartyKit server, no protocol prefix.
# Local dev: localhost:1999 (the default when unset).
# Production: code-detective.<your-github-username>.partykit.dev
NEXT_PUBLIC_PARTYKIT_HOST=localhost:1999
